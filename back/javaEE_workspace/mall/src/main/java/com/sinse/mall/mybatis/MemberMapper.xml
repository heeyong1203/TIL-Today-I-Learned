<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	
	<sql id="columns">
		member_id, sns_provider_id, id, password, name, email, regdate
	</sql>
	
	<!-- 아이디를 이용하여 회원정보 가져오기 -->
	<select id="selectById" parameterType="String" resultType="Member">
		select <include refid="columns"/> from Member where id=#{id} 
	</select>
	
	<!-- 가입 -->
	<insert id="insert" parameterType="Member">
		insert into member(sns_provider_id, id, password, name, email)
		values(#{snsProvider.sns_provider_id}, #{id}, #{password}, #{name}, #{email})
	</insert>
	
</mapper>